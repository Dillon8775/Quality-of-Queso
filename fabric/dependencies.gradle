dependencies {
    modImplementation(libs.balmCommon) {
        changing = libs.versions.balm.get().contains("SNAPSHOT")
    }
    modImplementation(libs.balmFabric) {
        changing = libs.versions.balm.get().contains("SNAPSHOT")
        exclude group: "net.blay09.mods", module: "shared-bridge"
    }
    modImplementation(libs.kumaFabric)

    // You can add other dependencies here as well.
    // Note that CurseMaven and Modrinth Maven, while convenient, does not include transitive dependencies.
    // If you are getting errors about missing classes in dependencies, try using their respective Maven repositories directly instead.
}
